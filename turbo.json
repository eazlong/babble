{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": { "persistent": true },
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": { "dependsOn": ["build"] },
    "lint": {}
  }
}
